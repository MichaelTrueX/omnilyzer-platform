<!--
File: docs/architecture/technology-decisions.md
Purpose: Records accepted, provisional, and rejected technology directions and validation needs.
Related:
- docs/adr/README.md
- spikes/README.md
- docs/architecture/platform-principles.md
-->

# Technology Decisions

Status labels in this document are deliberate: **ACCEPTED DIRECTION** constrains current planning, **TO VALIDATE** requires evidence before adoption, and **REJECTED / NOT CURRENTLY PLANNED** must not be introduced without reopening the decision through an ADR.

## Decision matrix

| Area | Candidate or direction | Status | Rationale | Validation needed |
|---|---|---|---|---|
| Platform architecture | Enterprise SaaS; modular monolith by default; API-first | ACCEPTED DIRECTION | Keeps boundaries explicit without premature distributed operations. | Validate boundary enforcement through later implementation reviews. |
| Backend | Python + Django 6 | ACCEPTED DIRECTION | Task 002 validated Django as the core backend foundation with PostgreSQL, transactions, migrations, application-service separation, security extension points, admin, OpenAPI adapters, and horizontally scalable topology. | Validate remaining deployment, background-work, and production-scale concerns independently; routine Django version upgrades remain subject to regression testing. |
| Backend alternatives | FastAPI for independently justified specialized services; NestJS not currently planned for core | ACCEPTED DIRECTION | Django showed no material core-platform blocker; maintaining multiple primary backend stacks without a demonstrated need would add complexity. FastAPI remains available when a specialized workload has a measured requirement. | Any specialized service or reopening of the core-stack decision requires its own evidence and ADR. |
| API framework | Django Ninja default; Django REST Framework approved fallback | ACCEPTED DIRECTION | Task 002 demonstrated equivalent behavioral/security contracts while Ninja provided a smaller type-led adapter and successful OpenAPI 3.1 to TypeScript generation. DRF remains the mature fallback. | Ninja upgrades must regression-test API behavior, generated OpenAPI/TypeScript, authentication/error behavior, and the PATCH omission/non-null workaround; concrete requirements may justify DRF. |
| Primary data store | PostgreSQL | ACCEPTED DIRECTION | Relational integrity, transactional behavior, and mature security features suit core platform data. | Validate the proposed queue use independently. |
| Tenancy | Shared schema with `workspace_id` | ACCEPTED DIRECTION | Task 003 validated shared-schema Workspace isolation across ORM, raw SQL, joins, aggregates, bulk operations, and connection reuse. | Final domain-model coverage, operational scale, and migration procedures remain implementation concerns. |
| Database isolation | PostgreSQL Row-Level Security (RLS) | ACCEPTED DIRECTION | Task 003 validated RLS with `FORCE ROW LEVEL SECURITY`, a non-owner `NOBYPASSRLS` runtime role, transaction-local Workspace context, fail-closed missing context, and cross-Workspace read/write isolation. | Validate external connection poolers, privileged and data migration procedures, the production credential lifecycle, and operational monitoring. |
| Identity | Keycloak | ACCEPTED DIRECTION | Task 004 validated real Keycloak OIDC discovery and real browser and native authorization-code flows while keeping Workspace authorization in Omnilyzer rather than Keycloak. | Validate production clustering and topology, HA, upgrade and recovery, MFA/passkeys, federation and social identity, multi-product SSO, secret rotation, operations, and exit strategy. |
| Identity protocols | OAuth 2 / OpenID Connect Authorization Code + PKCE | ACCEPTED DIRECTION | Task 004 validated confidential browser Code + PKCE S256, public native Code + PKCE S256, state and nonce, exact redirects, wrong-verifier rejection, and disabled implicit and password flows. | Validate production signing-key/JWKS rotation, coordinated logout and revocation behavior, and real native deep-link integration. |
| Browser authentication | BFF/server session, secure HttpOnly cookie, PostgreSQL-backed state | ACCEPTED DIRECTION | Task 004 validated server-side-only OAuth tokens, PostgreSQL-backed session persistence, a Secure, HttpOnly, host-bound cookie, CSRF, session-key rotation, server-side refresh, and local logout. | Validate production OAuth-token-at-rest protection, session TTL and revocation strategy, coordinated IdP logout, session cleanup and throughput, database failover and horizontal scale, and final HTTPS, domain, and cookie topology. |
| Public web | Next.js + React + TypeScript | TO VALIDATE | Candidate for public, SEO-sensitive websites. | Rendering, caching, accessibility, content workflow, deployment, security headers, and versioning. |
| Authenticated web | React + Vite + TypeScript | TO VALIDATE | Candidate focused application stack without coupling authenticated products to public-site rendering. | BFF integration, contracts, build outputs, accessibility, design-system consumption, and testing. |
| Frontend/design tooling runtime | Node.js 24 LTS | ACCEPTED DIRECTION | Task 005 exposed the Node 20 engine incompatibility and EOL baseline, then validated the current design-system toolchain on isolated Node.js 24.20.0 with strict engine enforcement. This does not change the Django or Python runtime. | Product and deployment runtime configuration, CI image/runtime pinning, routine Node LTS upgrades, and compatibility with future frontend packages. |
| Native mobile | React Native + Expo + TypeScript | TO VALIDATE | Candidate for iOS and Android while sharing contracts and domain concepts. | Secure OAuth storage/redirects, native capabilities, accessibility, updates, store builds, and design-system fit. |
| Design tokens | DTCG 2025.10 canonical semantic tokens + Style Dictionary transformation | ACCEPTED DIRECTION | Task 005 validated canonical DTCG-shaped sources, primitive/semantic separation, identical light/dark key sets, deterministic semantic web/native output, and a narrow fail-closed compatibility adapter. This does not imply complete DTCG support by Style Dictionary. | Final brand tokens, full DTCG feature needs, future compatibility-adapter removal, package/version distribution, visual regression, and native consumption. |
| Web components | Tailwind v4 semantic bridge; Radix primitives behind Omnilyzer wrappers | ACCEPTED DIRECTION | Task 005 validated reset default Tailwind design scales, semantic utilities, Button/TextField/Dialog wrappers, a controlled public boundary, semantic governance, automated accessibility evidence, and static styling. | Final component catalogue, actual product consumption, real-browser/manual accessibility, visual regression, versioned package distribution, and final CSP integration. |
| Background jobs | Django Tasks abstraction + PostgreSQL-backed production queue initially | TO VALIDATE | Could avoid premature queue infrastructure while retaining an abstraction boundary. | Delivery semantics, locking, retries, idempotency, scheduling, observability, throughput, and recovery. |
| Product observability contract | OpenTelemetry stable API/SDK + OTLP; structured JSON-compatible logging; Prometheus-compatible bounded application metrics; standard health, correlation, and build metadata; failure-isolated telemetry | ACCEPTED DIRECTION | Task 009 passed synthetic Django product-side validation, and ADR 0007 accepts the product-side observability contract only. No monitoring collector, storage, visualization, or other infrastructure backend is selected. | Validate production worker topology, collector/backend infrastructure, sampling, multiprocess metrics, real PostgreSQL integration, ASGI, internal `/metrics` network enforcement, production load, and production telemetry retention and operations. |
| Metrics scraper/query server | Prometheus server | ACCEPTED DIRECTION | Task 010 passed real Prometheus scrape/query, bounded-cardinality, private-network, failure-isolation, recovery, and Python multiprocess lifecycle validation, and ADR 0008 accepts Prometheus only as the initial metrics scraper, local TSDB, and PromQL/query component. This does not accept a complete monitoring platform or direct public Internet exposure of the operator surface. Production storage requires finite operational bounds, while exact retention and storage values remain unresolved. | Validate production service discovery; private/internal operator-access implementation; production target-network enforcement; actual WSGI/ASGI/process-manager integration; final build-info Gauge mode; rolling deployments; production scrape settings; supported/LTS version policy; exact storage and retention; capacity; HA, backup, and disaster recovery as requirements justify; production operational ownership; and upgrade/rollback runbooks. |
| Metrics visualization/operator interface | Grafana OSS | TO VALIDATE | Task 011 passed private Grafana OSS visualization/query, real Keycloak OIDC with PKCE, strict organization-role mapping, reproducible provisioning, failure isolation, and private server-side Prometheus access. It also proved that dashboard/folder visibility is not metric-data authorization: a Viewer can issue arbitrary PromQL against an available datasource. ADR 0009 is Proposed and pending architecture review. Grafana OSS is therefore a candidate only for a trusted operator population already authorized for the connected dataset. | Review ADR 0009; complete AGPLv3 legal review; confirm the trusted-operator visibility requirement and OSS auditability sufficiency; validate production private ingress, HTTPS, Secure cookies, datasource egress policy, persistence/database, supported-version upgrades, and operational ownership; and validate HA, backup, and disaster recovery as requirements justify. |
| Deployment artifact | Immutable OCI application images; build once and promote the same digest through stages | ACCEPTED DIRECTION | Task 007 validated build-once promotion, immutable digest references, the same candidate across isolated DEV, STAGING, and simulated PROD, runtime configuration and secret separation, readiness and migration gates, mutable-tag drift protection, and retained-digest rollback. | Validate independently reproducible OCI rebuilds, the production registry, signing and provenance, SBOM and vulnerability-scanning policy, actual PROD deployment, and multi-architecture needs if relevant. |
| Initial orchestration | Docker Compose or similarly simple orchestration with Nginx reverse proxy and blue/green slots | ACCEPTED DIRECTION | Task 007 validated isolated Compose projects, Nginx traffic switching, frontend/backend network separation, application-container hardening, and local zero-failure promotion and rollback mechanics. This is an initial-scale direction, not acceptance of Kubernetes or a permanent scaling solution. | Validate the actual production topology, multi-host availability and failover, production TLS and Cloudflare ingress, capacity, operations and monitoring, and whether or when a more capable orchestrator becomes justified. |
| Deployment migrations | Explicit serialized migration gate; expand-compatible changes for application rollback | ACCEPTED DIRECTION | Task 007 validated explicit PostgreSQL advisory-locked, checksum-tracked migrations, readiness gating, and rollback of old application code against an additive newer schema. It did not validate Django migrations themselves. | Validate the real Django migration implementation, migration ownership and credentials, large or long-running migrations, online migration techniques, destructive contract-stage governance, and backup/PITR integration. |
| Backup and PITR | pgBackRest | TO VALIDATE | Mature PostgreSQL-focused backup and point-in-time recovery candidate. | Encrypted backup, retention, restore drills, RPO/RTO, monitoring, and provider portability. |
| Platform distribution | Versioned standard wheel/npm packages from `omnilyzer-platform`; coordinated SemVer platform releases; exact product pins | ACCEPTED DIRECTION | Task 006 validated deterministic wheel/npm artifacts, independent 1.0/1.1 product adoption, same-major compatibility, exact pins, upgrade/rollback behavior, installed governance distribution, artifact/source integrity metadata, and no source copying. | Validate an actual private registry; publishing authentication and authorization; retention, availability, and disaster recovery; signed provenance and trusted publishing; real product repositories; production CI; Django reusable-app and database-migration compatibility; React/design-system production packaging; and long-term support/version policy. |
| Package registry / trusted publishing | Private standard package registry/service; provider TBD | TO VALIDATE | Task 006 deliberately remained registry-neutral and used local immutable artifacts only. Existing use of GitHub for source control does not select GitHub Packages. | Provider selection, authentication and authorization, trusted publishing, provenance/signing, availability, retention, replication/disaster recovery, costs, and operational ownership. |

## Other accepted directions

- `Workspace` is the tenant security boundary, with personal and organization Workspace types; users may belong to multiple Workspaces.
- Authentication and product-specific authorization are separate. Tenant isolation is strict.
- Public web and authenticated application surfaces are separate. Responsive web plus native iOS and Android are required.
- DEV, STAGING, and PROD are separate stages using pull requests and immutable releases; eventual horizontal scaling is required.
- Open-source and permissively licensed technology is preferred. Hosted GitHub and Cloudflare services are allowed, with vendor lock-in minimized.
- Security, auditability, privacy, compliance, and WCAG 2.2 AA are architectural concerns. Designs must accommodate relevant EU, US, and Asian regulation, including GDPR and Valoria data concerning minors or students.

## Rejected or not currently planned

- Evolving legacy Bisma or SHSLearningStudio into this platform, or importing other legacy implementation code.
- One global public Workspace for all consumer users.
- Schema-per-Workspace as the default.
- Microservices by default or Kubernetes at initial scale.
- Redis as a mandatory session store or memory-only sessions that prevent horizontal scaling.
- Permanent browser-token storage in `localStorage`.
- Manual copying or synchronization of platform source between products.
- Direct production source editing.
- Arbitrary per-product CSS systems.
- A large third-party SaaS boilerplate as the platform core.

Each validated major choice must be recorded through the [`ADR process`](../adr/README.md); a successful spike does not change status by itself. The accepted backend and API direction is recorded in [ADR 0001](../adr/0001-backend-framework-and-api-adapter.md), the accepted tenancy and database-isolation direction is recorded in [ADR 0002](../adr/0002-workspace-tenancy-and-postgresql-rls.md), the accepted authentication direction is recorded in [ADR 0003](../adr/0003-keycloak-bff-and-native-authentication.md), the accepted cross-platform design-system direction is recorded in [ADR 0004](../adr/0004-cross-platform-design-system-foundation.md), the accepted platform-packaging direction is recorded in [ADR 0005](../adr/0005-versioned-platform-packaging-and-distribution.md), the accepted immutable deployment and staged-promotion direction is recorded in [ADR 0006](../adr/0006-immutable-oci-deployment-and-promotion.md), the accepted product observability contract is recorded in [ADR 0007](../adr/0007-product-observability-contract.md), and the accepted initial metrics scraper/query direction is recorded in [ADR 0008](../adr/0008-prometheus-metrics-scraper-and-query.md). [ADR 0009](../adr/0009-grafana-operator-visualization.md) proposes Grafana OSS as the initial private visualization/query interface for trusted operators and is pending architecture review. ADR 0009 does not make Grafana a metric-data authorization boundary and does not select a complete monitoring platform. ADRs 0001 through 0008 remain accepted.
